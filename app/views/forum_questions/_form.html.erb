<%= form_for(@forum_question) do |f| %>
  <% if @forum_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@forum_question.errors.count, "error") %> prohibited this forum_question from being saved:</h2>

      <ul>
      <% @forum_question.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_area :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
